query AgendaTalksListItem($id: ID!) {
  talk(id: $id) {
    ...AgendaTalksListItem
  }
}

fragment AgendaTalksListItem on Talk {
  id
  title
  abstract
  type
  startTime
  endTime
  room {
    name
  }
  speakers {
    name
    avatar
  }
}
